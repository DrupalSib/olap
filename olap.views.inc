<?php

/**
 * @file
 * OLAP views hooks.
 */

function olap_views_data() {
//  if ($registers = register_get_all('RegisterTypeMovement')) {
//    foreach ($registers as $register) {
//      $schema = $register->getDbSchema('movement');
//      kpr($schema); die('asdf');
//
//      foreach ($fields as $field) {
////        $data[''] = $field;
//      }
//    }
//  }
//
//  return $data;
}
